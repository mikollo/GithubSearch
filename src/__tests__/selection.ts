// render app with search results

// find few Touchable elements (that make up rows) and invoke onPress on them and onPress on some
// again to test unselect

// find button which navigates to the next screen with list of all selected items and press it

// check snapshot

// mock Linking and invoke onPress on some item then check if
//  Linking.openURL was called with correct url
